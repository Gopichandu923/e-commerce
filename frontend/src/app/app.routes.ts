import { Routes } from '@angular/router';
import { HomeComponent } from './features/homecomponent/home.component';

export const routes: Routes = [
    {
        path: '',
        component: HomeComponent
    }
];
